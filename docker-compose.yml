version: "3.8"
name: e-pos

services:
    app:
        build:
            context: ./
            dockerfile: Dockerfile
        image: e-pos
        container_name: e-pos-container
        ports:
            - "8002:80"
        restart: unless-stopped
        networks:
            - e-pos

networks:
    e-pos:
        driver: bridge
